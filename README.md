# Clipboard Server 🖥️📋

## 1. Что это?

Небольшой HTTP‑сервер на Go для Windows, который:

* 📋 показывает текущий буфер обмена и позволяет его редактировать
* 📱 доступен с других устройств

Минимум кода, один `.exe`, никаких настроек безопасности.

---

## 2. Зачем это?

При прохождении задач на сайтах для поиска работы часто **обязательно использовать встроенное IDE**.

А оно, как правило:

* 😖 медленное
* 🧩 неудобное
* 🚫 без нормальных хоткеев

Поэтому я сделал простой интерфейс:

1. 💻 Пишу код на своём ноутбуке в нормальной IDE
2. 📋 Копирую код в буфер обмена
3. 🌐 Через браузер на другом устройстве буфер синхронизируется
4. 📎 Вставляю код в IDE на сайте

➡️ Это позволяет **работать комфортно** и при этом **обойти примитивные проверки на списывание**, не используя сторонние плагины или расширения.

---

## 3. Как настроить для использования на Windows ⚙️

### 🎥 Видео-инструкция

Подробная настройка с нуля:

👉 [https://www.youtube.com/@Lion-programmer](https://www.youtube.com/@Lion-programmer)

---

### 3.1 Запуск сервера

1. Соберите или скачайте `.exe`
2. Запустите файл
3. Введите порт (например `8080`)

В зависимости от того, **откуда вы подключаетесь**, используйте нужный адрес:

* 🖥️ **С этого же компьютера**:

  ```
  http://localhost:PORT
  ```

* 📱 **Из локальной сети (Wi‑Fi / LAN)**:

  ```
  http://192.168.0.X:PORT
  ```

  Локальный IP можно узнать командой:

  ```bash
  ipconfig
  ```

* 🌍 **Из любой точки интернета**:

  ```
  http://ПУБЛИЧНЫЙ_IP:PORT
  ```

  Публичный IP можно узнать на сайте 👉 [https://2ip.ru](https://2ip.ru)

---

### 3.2 Разрешение в брандмауэре Windows 🔥

При первом запуске Windows обычно спрашивает доступ — **разрешите для частных и публичных сетей**.

Если окно не появилось, добавьте правило вручную:

```powershell
netsh advfirewall firewall add rule name="ClipboardServer" dir=in action=allow protocol=TCP localport=8080
```

(замените порт при необходимости)

---

### 3.3 Настройка роутера 🌐

Чтобы приложение было доступно **не только в локальной сети**, необходимо:

1. Зайти в настройки роутера
2. Открыть раздел **Port Forwarding / Virtual Server / NAT**
3. Пробросить порт:

| Параметр        | Значение    |
| --------------- | ----------- |
| Внешний порт    | PORT        |
| Внутренний порт | PORT        |
| IP компьютера   | 192.168.0.X |
| Протокол        | TCP         |

После этого сервер будет доступен по вашему **публичному IP** с любого устройства.
